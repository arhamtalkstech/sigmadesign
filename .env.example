# SigmaDesign environment (optional)
# Copy to .env.local for local overrides. Never commit real secrets.

# Custom library root (default: ~/.sigmadesign)
# SIGMADESIGN_HOME=/path/to/your/library

# Node / Next
# PORT=3000
